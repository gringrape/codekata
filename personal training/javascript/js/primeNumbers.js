// 목적
// - 인수분해 하는 클래스를 선언 한다.
// - 함수를 property로 넣어주고 export 해준다.

let primeNumbers = {
    of(num) {
        if (num === 1) {
            return null
        }        
    }
}

module.exports = primeNumbers